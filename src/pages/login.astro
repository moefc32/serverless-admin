---
// @ts-nocheck
import { env } from 'cloudflare:workers';

import Layout from '../layouts/Layout.astro';
import Container from '../containers/Login.svelte';

const pageTitle = 'Login';
const turnstileKey = env.GUARD_TS_PUBLIC;
---

<Layout {pageTitle}>
    <Container {turnstileKey} client:only="svelte" />
</Layout>
