---
import Layout from '../layouts/Layout.astro';
import GenerateHash from '../components/GenerateHash.svelte';

const pageTitle = 'Settings';
---

<Layout {pageTitle}>
    <main class="flex flex-1 flex-col gap-9 mx-12 my-6 pt-25">
        <GenerateHash client:only="svelte" />
    </main>
</Layout>
