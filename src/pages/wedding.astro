---
// @ts-nocheck
import Layout from '../layouts/Layout.astro';
import PageTitle from '../components/PageTitle.astro';
import Container from '../containers/Wedding.svelte';

const pageTitle = 'Wedding';
---

<Layout {pageTitle}>
    <main class="flex flex-1 flex-col gap-3 mx-12 my-6 pt-24">
        <PageTitle {pageTitle} />
        <Container client:only="svelte" />
    </main>
</Layout>
